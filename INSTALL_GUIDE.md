# راهنمای سریع نصب Kiosk روی ویندوز

## 📦 فایل‌های مورد نیاز

پس از ساخت EXE، فایل‌های زیر را در یک پوشه قرار دهید:

```
C:\Kiosk\
├── kiosk.exe              ✅ فایل اجرایی (شامل staticfiles داخلش)
├── .env                   ✅ فایل تنظیمات (باید ایجاد کنید)
├── pna.pcpos.dll         ✅ اگر از کارت‌خوان DLL استفاده می‌کنید
└── (پوشه‌های زیر خودکار ایجاد می‌شوند)
    ├── media\             📁 فایل‌های آپلود
    ├── logs\              📁 لاگ‌های سیستم
    └── db.sqlite3         📄 دیتابیس
```

**نکته مهم:** 
- ✅ فایل‌های `staticfiles` (HTML، JavaScript، CSS، فونت‌ها) **داخل EXE هستند**
- ✅ دیگر نیازی به کپی کردن پوشه `staticfiles` نیست!
- ✅ فقط `kiosk.exe` و `.env` کافی است

## ⚙️ ایجاد فایل .env

**بله، فایل `.env` باید کنار `kiosk.exe` باشد!**

یک فایل با نام `.env` (بدون پسوند) در کنار `kiosk.exe` ایجاد کنید:

### محتوای فایل .env:

```env
# تنظیمات اصلی
SECRET_KEY=your-secret-key-change-this-123456789
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1

# سرور
SERVER_PORT=8000
SERVER_ADDR=127.0.0.1

# فروشگاه
STORE_NAME=فروشگاه من

# Payment Gateway
PAYMENT_GATEWAY_NAME=mock
MOCK_PAYMENT_SUCCESS=True

# Printer (اختیاری)
PRINTER_ENABLED=False
PRINTER_IP=192.168.1.100
PRINTER_PORT=9100
```

## 🚀 اجرا

1. **دوبار کلیک روی `kiosk.exe`**
2. **صبر کنید تا سرور راه‌اندازی شود**
3. **مرورگر را باز کنید:** `http://localhost:8000`

## 👤 ایجاد کاربر مدیر

برای دسترسی به پنل ادمین:

```cmd
cd C:\Kiosk
kiosk.exe createsuperuser
```

سپس به `http://localhost:8000/admin` بروید.

## 📝 نکات مهم

- ✅ فایل `.env` **حتماً** باید کنار `kiosk.exe` باشد
- ✅ `SECRET_KEY` را حتماً تغییر دهید
- ✅ برای production، `DEBUG=False` تنظیم کنید
- ✅ فایل `db.sqlite3` خودکار ایجاد می‌شود

## 🔧 عیب‌یابی

### EXE اجرا نمی‌شود؟
- Visual C++ Redistributable نصب کنید
- با دسترسی Administrator اجرا کنید

### سرور شروع نمی‌شود؟
- پورت 8000 آزاد باشد
- فایل `.env` وجود داشته باشد
- لاگ‌ها را در `logs/` بررسی کنید

### جزئیات بیشتر؟
به `docs/INSTALL_WINDOWS.md` مراجعه کنید.

